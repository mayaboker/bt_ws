# bt-msgs

Shared MessagePack message types for BT workspace processes.

`TrackerResultMessage` currently carries the detector frame sequence and its
GStreamer presentation timestamp. Additional tracker result fields can be
added as the shared detector protocol is developed.

