```bash title="remove gui"
sudo systemctl set-default multi-user.target
sudo systemctl disable --now sddm
sudo reboot
```

```
Bullseye 5.10
https://docs.radxa.com/en/zero/zero3/download
sudo rsetup
```